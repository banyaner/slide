
window.onload = function () {
    var slide = new Slide('#slide', {
        //autoplay: 2000,
        //direction: 'vertical',  // vertical, horizontal
        button: true   //设置是否有按钮
    });
    window.slide = slide
}